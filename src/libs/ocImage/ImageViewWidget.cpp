#include "ImageViewWidget.h"

ImageViewWidget::ImageViewWidget(QWidget *parent)
    : QWidget{parent}
{

}

void ImageViewWidget::setup()
{
    Q_ASSERT(!"MUSTDO");
}

void ImageViewWidget::enable(const ToolNames tools, const bool is)
{
    Q_UNUSED(tools);
    Q_UNUSED(is);
    Q_ASSERT(!"MUSTDO");
}
