#include "ResourceData.h"

void ResourceData::clear()
{
        mMainData.clear(); //, mInputMask.clear(), mOutputMask.clear(),
                // mMeanFace.clear(), mWeightMask.clear(), mLayerSet.clear();
}
